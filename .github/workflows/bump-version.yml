name: Bump Version

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Version bump to apply
        required: true
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major
          - custom
      version:
        description: Custom version, e.g. 1.2.3 or v1.2.3-beta
        required: false
        type: string

permissions:
  contents: write

jobs:
  # Bumps all version files, commits, and pushes the tag. The release job calls
  # release.yml directly because pushes made with GITHUB_TOKEN do not trigger
  # other workflows. ci.yml skips the master branch push for these bump commits.
  bump:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.bump.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        id: bump
        run: |
          if [[ "${{ inputs.bump }}" == "custom" ]]; then
            TARGET="${{ inputs.version }}"
          else
            TARGET="${{ inputs.bump }}"
          fi

          if [[ -z "$TARGET" ]]; then
            echo "Custom version is required when bump=custom" >&2
            exit 1
          fi

          bash ./scripts/bump-version.sh "$TARGET" --push
          VERSION="$(node -p "require('./package.json').version")"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Pushed tag v${VERSION}; starting release.yml."

  release:
    needs: bump
    uses: ./.github/workflows/release.yml
    with:
      tag: ${{ needs.bump.outputs.tag }}
    secrets: inherit
